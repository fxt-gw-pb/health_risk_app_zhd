# 交付包 · "智评慢病"演示流程宣传片（给 Claude Design）

这是一个**全新、自包含**的交付包。请**只看本文件夹**，无需参考其他历史宣传材料。

## 你要做什么（一句话）
做一个**横屏（1920×1080）自动播放的 `promo.html`**：一侧"竖屏手机"里**动态演示**智评慢病小程序"主页 → 对话答题 → 风险报告 → 自由对话 → 进阶评估（含诊断提醒）"的完整流程；另一侧用**解说面板文字**讲清"它是什么 / 怎么用"。**手机里的界面是用代码高保真重画并按脚本演出来的（演示套壳），不是截图、不跑真实接口。** 播放过程本身即视频，**不做字幕、默认不配音**，约 45–50 秒，科技风、表现力强但克制、不要 AI 套话味。

## 先读哪个
1. **`01-视频创意与规格.md`** —— 创意、风格、规格，以及 ★ "可逐帧捕获"的技术要求（务必满足，便于后续转 MP4）。
2. **`02-小程序UI还原规范.md`** —— 每一屏的像素级还原（配色/渐变/字号/圆角/组件/**真实中文文案**）。这是把手机画"像"的关键。
3. **`03-分镜脚本与时间轴.md`** —— 6 个场景、逐秒编排：手机演什么 + 解说面板说什么。

## 机器可读内容（`content/`）
- `demo-script.json` —— 手机内分步动作脚本（打字/点选/确认条/报告展开…，含相对时间）。
- `narration-panels.json` —— 解说面板文案（每场景 标题 + 要点 = 要讲的话上屏）。
- `report-data.json` —— ⚠️ **报告里的真实计算数据**（百分比、Top5、模型透明度、诊断提醒）。由冻结医学内核算出，**请勿手改任何数字**。

## 资源（`assets/`）
- `qrcode.png`(1000×1000) / `qrcode.svg` —— 收尾扫码用，指向 `https://fxt-gw-pb.github.io/health_risk_app_zhd/`。
- 品牌 Logo 用 `assets/logo.png`（蓝青心形＋脉冲标志，已抠图、调色、透明底）；直接放置即可，可选加白色/极浅圆形或圆角底。

## 演示主角（脚本里已固定）
李先生，男，58 岁，身高 174 / 体重 86（BMI 28.4 肥胖），有糖尿病家族史等。第一层报告：糖尿病 6.6% 低危 · 心血管病 21.8% 高危 · 高血压 21.4% 高危；进阶到血液检查后空腹血糖 7.4 触发 **2 型糖尿病诊断提醒**（糖尿病卡变"疑似已患"）。

## 必须遵守
- 中文文案**逐字使用**（已是产品真实文案）。
- 报告**数字以 `report-data.json` 为准**，不要编造。
- 满足 `01` 里"可逐帧确定性捕获"的 6 条要求。
- 交付 **一个可离线打开的 `promo.html`**（字体/图片内联或随附 assets）。
